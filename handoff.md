# Current handoff — 2026-09-19

Repo: C:\work\code\minecraft\interstellar\interstellar. Branch codex/world-mesh-reference; origin https://github.com/rohrl/mc-interstellar.git. JDK C:\Portable\jdks\temurin-21.0.12.1; Fabric/Minecraft1.21.1. Read AGENTS.md. Normal edits, commits/pushes and autonomous runtime inputs authorized. No force push or subagents. Preserve worlds/.idea/secrets and owner edits.

## Priority and preserved work

Owner loves current appearance; optimize FPS with minimal fidelity loss. Packaging paused on codex/demo-packaging-wip at2fe8674 (pushed, unverified, never built/launched). Original AA WIP8ad46eb remains on codex/terrain-antialiasing; do not cherry-pick its obsolete renderer. Current2xAA is already adapted. Owner rejected soft EDGE blur; retain sharp default. Rain/snow, teleport support and star differences deferred; owner handles movement/flicker acceptance. Never revive rejected ordinary-camera background overlays.

## Active performance goal: at least30FPS at1440p

Owner explicitly rejected stopping at20FPS and renewed the minimum target to30FPS, ideally60+, at1440p without noticeable quality loss or correctness compromises. Continue optimization; the goal remains active.60FPS at1440p is still not reached: latest original-program frozen wall frame median49.5–49.6ms (~20FPS), another downward scene82.6ms (~12FPS). Compact nodes are now accepted (D056); retain midpoint trees, sharp2xAA, original optical steps/quality. GPU p95 gains12.4–12.5% wall/19.6% down; medians~23/~15FPS still miss30. Continue toward the active target.

Rejected experiments are separately preserved/pushed:
- codex/terrain-sah-experiment at ba0bd73:1.9% wall/3.3% downward GPU gains versus3.7–4.1x CPU tree build cost. docs/experiments/terrain-sah.md.
- codex/empty-step-experiment at220b883:conversion-only variant saved0.5%; larger empty-space steps looked favourable against a uniform-disabled version of themselves but were2.5% slower against the separately compiled original, confirmed in reverse order. docs/empty-spans.md. Neither F/Shift+F nor the original-reference shader exists in production.

Review found no confirmed new correctness defect. Detailed performance evidence and larger unproven options are in docs/aa-performance.md; next investigate repeated shading and geometry-data fetches with matched compiled references. Precipitation was assessed and deferred: native geometry is small, but transparent ray-ordered composition and wet-weather GPU cost need additional work. docs/precipitation-assessment.md records a plausible camera-local grid/transmittance design without claiming measured weather overhead. docs/relativity-feature-ideas.md proposes clocks, opt-in relativistic devices, orbit probes and light-echo beacons. They are proposals, not implemented or newly committed scope. Existing packaging/horizon crossing/actual-player-body requirements remain.

Final restored build: performance-review-build.log succeeds; runtime performance-review-runtime.log. Original experimental client PID1648 closed normally; only one final client launched. Both native/general programs pass17600 sampled comparisons each, zero mismatches/inconclusive/unresolved. Native image after diagnostics inspected. Build retains51 passing tests. See docs/mesh-ray-validation.md for exact scope; ordinary rendering has no added work.

Fullscreen F11 proved unreliable. Local ignored run/size-minecraft.ps1 sets an exact2560x1440 client area; -Restore returns the saved870x519 outer window (854x480 client). Backup persists, so check bounds before reusing it in another session. Use logged framebuffer dimensions as authority. run/measure-pass.ps1 -Log LOG starts B and gates on completion; -ClearPrevious clears an old result; -Key123 uses F12. Timeout means inspect state, not restart. Desktop CopyFromScreen sometimes returns white while Minecraft's saved framebuffer is correct; use native F2 when diagnosing this. Initial source inspection is needed after launching: /interstellar inspect 14 300 14, then F9. Gate on Terrain snapshot and World mesh ready/Streaming terrain ready logs before further controls.

## Verified production checkpoint

Native meshes use a dedicated shader with MeshMode fixed to1; both native/general programs import shaders/include/terrain_shared.glsl. Same optics/traversal/materials/AA and all earlier toggles. F9 S switches programs; Shift+S captures general versus selected in one frame. Native is the default in F9 meshes/F10; general remains the voxel backend/reference. Keep shared GLSL as the single optical implementation; shader JSON manifests differ in their active samplers/uniforms.

shader-split-build.log:51 tests pass; final resource build also passes. shader-split-runtime.log: each program passes17280 sampled CPU/GPU checks (180 distinct directions over mode/layout variants), zero mismatch/inconclusive/unresolved. Three pairs across monolithic/streamed wall/downward views are pixel-identical; downward candidate inspected. Reversed-order1440p streamed wall GPU p95=61.011→59.059ms and61.424→59.319ms (3.2–3.4%). Small wall1.9% gain; no meaningful downward gain. Fullscreen native frame median~58.5ms (~17FPS), still below target and affected by cap/vsync. No cross-session/universal-FPS or register-occupancy claims. Evidence and review findings: docs/aa-performance.md. No block/time/weather/config edits.

Review found no confirmed new correctness bug in the checked cache certificates, address ranges, shader state or source-shift bounds. The later experiments above supersede the previous immediate tree/stepping plans. Further larger rendering proposals remain unproven; no accepted lensing/world-integration or quality change is required.

Earlier defaults remain:2xAA, adaptive paths, fast bounds/addressing, learned empty-region half-extent1024 with unchanged conservative clipping/publication and per-ray resets. F9 I/Alt+Shift+P compares cache; Shift+I/Ctrl+Alt+P compares16/1024 extent. Extent controls empty-space reuse, not capture distance. Extra fixed-cell occupancy-query prototype was slower and discarded. Earlier measurements: docs/aa-performance.md. Remaining features: fluids, transparent/special blocks, non-living/block entities, shadows/eye glow/glint/translucent layers. Packaging/deeper relativity/player-body remain later.

## Runtime architecture

F10 retains chunk BVHs in GPU row arenas; native section/light and chunk events queue bounded5ms capture slices. Publish individual replacements; retain overlapping chunks on movement. Actors/clouds use a separate per-frame BVH and reusable textures/staging buffers. Both trees share curved nearest-hit/cloud ordering. Source refresh retains terrain and automatically pauses/resumes; initial source inspection still manual. Camera guard256 plus exterior/source-availability guards. F10 off frees the cache, so reactivation recaptures (~35–40s).

GPU arenas:4095-wide RGBA32F,16384 triangle rows/4096 node rows (~1280MiB), first4 node rows reserved for top-level index. RowArena coalesces frees, no defragmentation fallback.7M terrain triangle cap; render distance above16 refused. Camera window=render distance+1 chunks each side, full height, loaded chunks only. Monolithic/moving textures4096 wide. Details: docs/streaming-terrain.md, live-native-mesh.md, viewing-range.md. Independent fixture scope: docs/mesh-ray-validation.md; boxes/sampled hit cells, not arbitrary-material certification.

## Scene and efficient checks

Owner source anchor(14,300,14), N65, r_s8.125, COM approximately(16.01,302.04,15.99). **Do not restore old N64.** Test player pose(16.5,302,-45.5), yaw.281/pitch.91. Owner may move/close client; check process/log before inputs. Never launch two clients against the save.

Launch with JAVA_HOME above: .\gradlew.bat -I run/stable-init.gradle runClient, redirect logs. Gate on joined-the-game before setup commands (shader startup may take longer than a fixed wait). Fresh player may need spectator→test teleport→creative for flight. Setup teleport is not v1 teleport acceptance. GUI/Gradle need escalation.

F9 pauses; wait snapshot, then Shift+M streamed or M monolithic. Switching backend recaptures while paused and sets lensing OFF: Space restores it. Gate on snapshot/mesh ready logs, never press M twice during capture. SendKeys Shift+M once arrived as plain M; holding native Shift around native M was reliable. F3+T shader reload needs normal play; wait for the new snapshot to finish before M (compilation can delay it). F9 beyond128 automatically requires streamed mesh. C independent fixture; B benchmark (120 warmup/300 samples); F12 live benchmark. B first clears an already-completed benchmark; the next B starts another. Gate on new completion count, and use logged dimensions/settings; resize or other input can cancel a run. AA A; adaptive G; bounds T; addressing R; shader S/Shift+S. P vanilla/unbent; Shift+P4-ray quality; Ctrl+P original/adaptive; Ctrl+Shift+P bounds; Alt+P addressing; Alt+Shift+P empty regions; Ctrl+Alt+P extent. E entities/N clouds/J fine/Q scale. Comparisons require F9 pose to match actual player camera; metadata records settings. F11 fullscreen; restore small window after tests.

Local ignored run/control-short.ps1 forwards the GUI helper to reduce repeated command text. Helpers: C:\Users\karol\Documents\Codex\2026-09-13\i-want-to-create-a-minecraft\work\control-minecraft.ps1 and capture-minecraft.ps1; held native keys120ms. run/send-safe-command.ps1 preserves clipboard. Comparator: java tools/CompareAppearance.java PAIR_DIRECTORY; three-path mode for arbitrary images. Logs/captures/saves ignored. Prefer numeric pairs and bounded logs; one milestone image, no repeated screenshot polling. Historical results are in feature docs/progress/decision log, not duplicated here.

## Latest accepted compact-node checkpoint

Exact compact BVH nodes are accepted; docs/compact-nodes.md records format, overflow handling, measurements and costs.53 build tests,26240 sampled GPU comparisons per compact/original program, zero mismatch/inconclusive/unresolved. Three1440p pairs pixel-identical; downward inspected. GL_ARB_shader_bit_encoding capability guard retains original rendering on unsupported GPUs (fallback reviewed/built, not run on second hardware). Original and compact node textures coexist for comparisons: extra268MB streamed mirror and duplicate uploads remain an explicit tradeoff. F9 F switches complete native programs; Shift+F compares. Main commit/push in progress for this checkpoint.

Final client: compact-nodes-final-runtime.log, exec session70101, live F10 active, small870x519 window, wall pose(16.5,302,-45.5), yaw.281/pitch.91, N65.600 changing actor updates, original allocations1/1, small live GPU p95=12.355ms/frame median12.721ms; not a1440p comparison. Runtime has all renderer/test changes; the later capability-guard-only build compact-nodes-final-build.log passes and leaves this supported RTX path unchanged. Previous client closed normally before launch; no concurrent clients, no block/time/weather edits. Next experiment: compile a shader specialized for ordinary live settings/diagnostics off, retaining a separately compiled reference and existing quality/toggles.

Final-hit shading rejected:2.5% slower with identical pixels; preserved/pushed codex/final-hit-shading-experiment9967e4b, docs/experiments/final-hit-shading.md. Earlier rejected experiments remain separate.

Local ignored run/control-short.ps1 now provides real scan codes for Escape/arrows; old VK-only events did not reach those bindings. Other keys still forward. run/setup-comparison.ps1 -Log LOG gates a fresh client's join, inspects the existing N65 source, opens F9 and requests streamed capture. Wait for Streaming terrain ready; Space restores lensing. Same-renderer comparisons can rotate via arrows; metadata records preview angles and benchmarks now log yaw/pitch. Vanilla P still requires actual player pose. Native F2 is reliable when desktop CopyFromScreen shows white. Unchanged-camera measurements accidentally attempted with old arrow input are documented as wall repeats, never downward results.
